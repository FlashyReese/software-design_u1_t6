plugins {
    id 'java'
}

group 'me.yaochung'
version 'PROTOTIPO-FINAL'

repositories {
    mavenCentral()
}

dependencies {
    testImplementation 'org.junit.jupiter:junit-jupiter-api:5.6.0'
    testRuntimeOnly 'org.junit.jupiter:junit-jupiter-engine'
}

jar {
    manifest {
        attributes "Main-Class": "me.yaochunghu.u1_t6.Main"
    }

    from {
        configurations.compile.collect { it.isDirectory() ? it : zipTree(it) }
    }

    from "LICENSE.txt"
}

test {
    useJUnitPlatform()
}